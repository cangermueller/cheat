* Fast, efficient binary compression, RPC calls

# file.proto
* go/protodosdonts

syntax = '2.0';
option py_api_version = 2;

message Person {
  optional string first_name = 1;
  optional string last_name = 2;
  optional int32 age = 3
  }

message group {
  repeated Person people = 1;
  }


# BUILD file

proto_library (
  name  = 'data_libarary',
  srcs = ['file.proto']
  )

py_proto_library (
  name = 'data_library_py_pb2',
  deps = [':data_library']
  api_version = 2.0
  )

