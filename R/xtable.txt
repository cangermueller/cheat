# Options
options(xtable.type='html') // change output from 'latex' to 'html'

# xtable
xtable(tab, ...)
  caption=
  label='tab:x'
  digits=c(0, 2)

# print.xtable
print(xtable, ...)
  table.position='!h'
  include.rownames=F
