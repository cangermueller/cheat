# Installation
virtualenv venv
pip install pyscaffold
putup myproject

# Features
* versioneer package: updated version after git -a -m v0.1
* pytest support  // setup.py test
* sphinx support  // setup.py docs

MANIFEST.in // included for sdist/bdist
requirements.txt  // dependent packages
git tag -a -m 'First version' v0.1  // create annotated version
